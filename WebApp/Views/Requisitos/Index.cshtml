@model IEnumerable<WebApp.Models.Requisito>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EdadMinima)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EdadMaxima)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ScoringMinimo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CantidadAvales)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CantidadRecibosSueldo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EdadMinima)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EdadMaxima)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ScoringMinimo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CantidadAvales)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CantidadRecibosSueldo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
